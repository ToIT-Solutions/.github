HOSTING FOR WEBDEV

next.confit.ts should have:

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
};

export default nextConfig;


-REMOVE THE FOLDERS AND JUST MAINTAIN THE HTML FILE NAMES THEN HOST , SHOULD WORK LIKE THAT
